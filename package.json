{
    "author": "",
    "description": "- `develop` branches from `master` - `release/*` should branches from `develop` - `release/*` branch should merged in `master`, backwards `develop`",
    "keywords": [],
    "license": "ISC",
    "main": "index.js",
    "name": "release",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "version": "value41"
}
